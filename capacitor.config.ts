import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sintonia_player',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
